spring:
  profiles:
  - dev #当前的使用环境
---

spring:
  profiles: dev #开发环境
  application:
    name:config-server-dev
---
spring:
  profiles: test #测试环境
  application:
    name:config-server-test
  